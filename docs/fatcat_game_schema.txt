📦 FATCAT GAME SCHEMA — For insert_game and game tracking

This is the standard structure used in the `games` collection.
FatCat should follow this format when creating or managing games.

{
  groupId: int — Telegram group ID where the game is hosted,
  hostTelegramId: int — Telegram ID of the game creator,
  gameType: str — type of game ('trivia', 'raffle', 'challenge', etc.),
  status: str — 'in_progress', 'completed', or 'cancelled',
  pointType: str — 'ranking' or 'per_action',
  pointValues: dict — scoring rules:
    • if pointType is 'ranking': { '1': 20, '2': 10, '3': 5 }
    • if pointType is 'per_action': { 'correctAnswer': 5, 'participation': 1 },
  players: dict — per-user stats, by telegramId:
    {
      "123456": {
        username: str,
        score: int,
        actions: optional list (e.g. ['correctAnswer', 'voted'])
      },
      ...
    },
  winners: list — once finalized, list of:
    [
      { telegramId: str, rank: int, points: int },
      ...
    ],
  createdAt: ISODate
}
